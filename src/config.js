// API Configuration
// Your computer's IP address: 192.168.0.101

// IMPORTANT: Choose the right URL based on where you're testing:

// Option 1: For Android/iOS Emulator (localhost won't work on emulator)
// export const API_BASE_URL = 'http://10.0.2.2:8000';

// Option 2: For Physical Device OR Web Browser (RECOMMENDED - works for both)
export const API_BASE_URL = 'http://192.168.0.101:8000';

// Option 3: For Web Browser only (won't work on physical device)
// export const API_BASE_URL = 'http://localhost:8000';
